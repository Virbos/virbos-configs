##############
## vb-zplug ##
##############

# Basic zsh "plugin" manager for Virbos.
# Set a few environment variables and source this script,
# and it'll load all the modules you want.
# Modules go in ~/.local/share/vb-zplug/modules

# Constants
MODPATH="$HOME/

# Enable colors and change prompt:
autoload -U colors && colors	# Load colors
setopt autocd		# Automatically cd into typed directory.
stty stop undef		# Disable ctrl-s to freeze terminal.
setopt interactive_comments

# Load modules.
if [ -z "$VB_USE_VI" ]; then
	
fi
